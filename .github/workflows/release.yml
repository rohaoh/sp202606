name: Release

on:
  push:
    tags:
      - 'v[0-9]*'
  workflow_dispatch:
    inputs:
      tag:
        description: '릴리즈할 태그 (예: v2.3.0)'
        required: true
        default: 'v2.3.0'

permissions:
  contents: write

jobs:
  # ── 아키텍처별 빌드 (x64 / arm64) ─────────────────────────────────
  build:
    name: Build (${{ matrix.arch }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        arch: [x64, arm64]

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.arch == 'arm64' && 'amd64_arm64' || 'amd64' }}

      - name: Install CMake
        run: choco install cmake --installargs 'ADD_CMAKE_TO_PATH=System' -y
        shell: cmd

      - name: Install dependencies
        run: npm ci
        working-directory: codes/2026-7-7(1)
        env:
          HUSKY: '0'

      - name: Build native addon (${{ matrix.arch }})
        run: npx cmake-js compile --arch ${{ matrix.arch }}
        working-directory: codes/2026-7-7(1)
        env:
          npm_config_arch: ${{ matrix.arch }}

      - name: Bundle renderer
        run: node scripts/bundle.js
        working-directory: codes/2026-7-7(1)

      - name: Build installer (${{ matrix.arch }})
        run: npx electron-builder --win nsis msi --${{ matrix.arch }} --publish never
        working-directory: codes/2026-7-7(1)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ matrix.arch }}
          path: |
            codes/2026-7-7(1)/dist/*.exe
            codes/2026-7-7(1)/dist/*.msi
          retention-days: 1

  # ── Draft 릴리즈 생성 + 아티팩트 첸부 ──────────────────────────
  release:
    name: Create Draft Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download x64 artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-x64
          path: dist

      - name: Download arm64 artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist-arm64
          path: dist

      - name: List dist files
        run: ls -lh dist/

      - name: Create Draft Release & upload files
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag || github.ref_name }}
          draft: true
          name: Physics Simulator ${{ github.event.inputs.tag || github.ref_name }}
          body: |
            ## Physics Simulator ${{ github.event.inputs.tag || github.ref_name }}

            ### 다운로드
            | 파일 | 아키텍처 | 형식 |
            |------|----------|------|
            | `*-x64.exe` | Intel / AMD 64-bit | NSIS 설치 마법사 |
            | `*-x64.msi` | Intel / AMD 64-bit | Windows Installer |
            | `*-arm64.exe` | ARM64 (Surface Pro X 등) | NSIS 설치 마법사 |
            | `*-arm64.msi` | ARM64 | Windows Installer |

            ### 설치 방법
            - **.exe**: 더블클릭 → 설치 마법사 진행
            - **.msi**: 더블클릭 → 자동 설치 (기업 배포용)
          files: dist/*
          fail_on_unmatched_files: true
          generate_release_notes: false
