# 🪂 낙하 물리 시뮬레이터 — 릴리즈 노트

**버전:** `3.0.0`  ·  **플랫폼:** Windows (x64, exe/msi)  ·  **빌드:** Electron + C++(N-API) 물리 엔진

하늘에서 무언가 떨어진다면? **종단속도 · 충격량 · 파괴율**을 실시간 3D로 계산·시각화하는 시뮬레이터입니다.

---

## ✨ v2.0.0 → v3.0.0 핵심 변경 사항

### 1. 물리 엔진 고도화
- **Reynolds 수 기반 드래그**: 저속(Re<1000) 구간에서 Cd를 동적으로 보정해 정확도 향상
- **물 저항**: 수심(waterDepth) 입력 시 공기보다 20배 강한 저항력 적용
- **Coriolis 효과**: 위도 입력에 따른 지구 자전 편향 계산 (동/서 편향 거리로 표시)
- **고도별 중력 변화**: WGS84 모델 기반, 극지방/적도 중력 차이와 고도에 따른 감소 반영
- **회전 감쇠**: 공기 저항으로 스핀(RPM)이 시간에 따라 점차 줄어듦
- **바운스 물리**: 재충돌 시 에너지 손실 계산, 연쇄 바운스 횟수/속도 추적
- **에너지 손실 추적**: 낙하 전체 과정의 기계에너지 손실량 누적 기록

### 2. 다중 물체 시간차 낙하 + 물체간 충돌 (신규)
- Object 2/3/4 각각에 **낙하 시간차(초)** 입력 — 동시 낙하가 아니라 순차적으로 떨어뜨리는 연출 가능(기본값 0 = 기존과 동일)
- **시간차 자동 적용** 버튼으로 0, step, 2×step 초 간격을 일괄 설정
- **물체간 충돌(시각 효과)** 토글(기본 OFF) — 물체끼리 겹치면 서로 밀어내는 시각적 보정. 핵심 물리 계산·충돌 결과 수치는 그대로 유지됨
- 다중 물체 낙하 계산을 **Web Worker 풀로 병렬 처리** — 물체 수가 늘어나도 메인 스레드가 버벅이지 않음

### 3. 프리셋 · 결과 내보내기 강화
- 프리셋을 **JSON 파일로 내보내기/가져오기** (기존 localStorage 저장/불러오기에 추가) — 다른 PC와 공유하거나 백업 가능
- CSV 내보내기에 **다중 물체 요약 섹션** 추가: 물체별 질량·면적·Cd·시간차·종단속도·충돌속도·낙하시간·착지시각

### 4. 전체적인 UI 다듬기
- 다크 테마에 맞춘 통일된 스크롤바
- 키보드 포커스 표시(`:focus-visible`) — 키보드 탐색 접근성 개선
- 버튼 클릭 시 눌림 피드백 애니메이션
- Multi-Object/히스토리 카드 hover 시 테두리·그림자 강조
- 좌측 패널 섹션 접힘/펼침 부드러운 전환

### 5. 자동 업데이트
- Help 메뉴에 **Check for Updates…**(수동 확인) + **시작 시 자동으로 업데이트 확인**(토글, 기본 OFF) 추가
- GitHub Releases를 배포처로 사용 — **이 v3.0.0 릴리즈부터 실제로 동작**
- exe(NSIS)로 설치한 경우에만 자동 업데이트 지원. msi는 자동 업데이트 대상이 아니며, 새 버전이 나오면 GitHub Releases에서 새 msi를 받아 수동 재설치 필요

### 6. 배포 자동화 (개발 인프라)
- **GitHub Actions로 태그 push / 수동 실행 기반 자동 빌드+배포** 구축 — Windows 클라우드 러너에서 `npm install` → C++ 컴파일 → 번들링 → exe/msi 패키징 → GitHub Releases 업로드까지 전 과정이 자동으로 처리됨
- **exe(NSIS) + msi 두 가지 설치 형식 동시 제공** — 개인 배포(exe, 자동 업데이트 지원)와 사내/그룹정책 배포(msi) 모두 지원
- **앱 아이콘 신규 추가** (다크 테마에 맞춘 낙하공 모티프) — 이전에는 아이콘이 없어 Electron 기본 아이콘으로 표시되고 MSI 빌드도 실패했음

### 7. 코드 품질 · 안정성 (개발자용)
- TypeScript 타입 체크 활성화(`checkJs`) — 검증 과정에서 실제 잠재 버그 2건 발견 및 수정
  (죽어있던 중복 코드 두 곳: `main.js`의 불필요한 재-require, `parallel-physics.js`의 중복 선언되어 항상 덮어써지던 버그 함수)
- C++ 물리 엔진 핵심 계산에 대한 **단위 테스트 24개** 추가 (cmake-js/Electron 헤더 없이 `g++`만으로 독립 실행 가능)
- 빌드 캐싱(변경 없으면 C++ 재컴파일 스킵), 보안 감사, 번들 분석, 성능 벤치마크, 메모리 누수 감지, API 문서 자동 생성 등 개발 도구 10종 추가
- CI 환경에서 `npm install`이 항상 실패하던 `husky` 관련 버그 수정

---

## 🧩 전체 기능

> 모든 기능은 좌측 패널/툴바의 **토글 스위치**로 개별로 켜고 끌 수 있습니다.

### 물리 시뮬레이션
- **종단속도 / 운동방정식**: `v_t = √(2mg / ρCdA)`, `ma = mg − ½ρCdAv²`
- **ISA 표준 대기**: 고도별 공기 밀도·온도·압력 자동 계산
- **바람 / Magnus 효과 / 발사체 모드 / 지형(평지·경사면·수면·고지대)**
- **공력 가열**: 정체 온도 · Sutton-Graves 열유속 · 복사 평형 표면 온도
- **다중 물체**: 최대 3개 물체 독립 시뮬레이션 + **시간차 낙하** + **물체간 충돌(시각 효과)** (v3.0.0 신규)
- **Reynolds 수 기반 드래그 보정 · 물 저항 · Coriolis 효과 · 고도별 중력 · 회전 감쇠 · 바운스 에너지 손실** (v3.0.0 신규)

### 충돌 · 파괴
- **임계 기반 파괴**: 충격 압력이 재질 항복강도를 넘을 때까지 버팀(Withstood), 넘으면 점진적 파괴
- **타깃 두께·항복강도 직접 입력**, **버팀 시 바운스**, **충격 에너지 대시보드**
- **파편 / 분화구 / 먼지**: InstancedMesh 기반 고성능 렌더링, 파편끼리 충돌 판정
- **재료별 파편 차등**(유리=잘게, 금속=3조각 분리, 목재/콘크리트=청크), **타겟 바닥에서 띄우기**

### 3D 모델 (GLB / STL)
- 프리셋 모델 3종, 파일 업로드(assets 자동 복사), 모델 크기 조정(비율 유지), Multi-Object GLB

### 시각화 · 편의
- 카메라 프리셋/추적, 실시간 그래프, 단위 전환(SI/km·h/제국단위), 궤적 라인·자료 표·CSV/PNG 내보내기
- **프리셋 저장/불러오기(localStorage) + JSON 파일 내보내기/가져오기**(v3.0.0 신규)
- 온디맨드 녹화/재생, 설정창 폭 드래그 조절
- 다크 테마 통일 스크롤바·포커스 표시·클릭 피드백·hover 강조(v3.0.0 신규)

### 배포 · 업데이트 (v3.0.0 신규)
- **자동 업데이트**(electron-updater, exe 전용) · **GitHub Actions 자동 배포**(태그/수동 트리거) · **exe + msi 동시 제공**

---

## 💾 설치 및 실행 (Windows · x64)

> 처음 한 번만 Node.js, CMake, Visual Studio Build Tools(C++) 설치가 필요합니다.

```bash
cd codes/2026-7-1(4)
npm install
npm run build-addon   # C++ 물리 엔진 컴파일 (cmake-js)
npm start
```

설치 파일(exe + msi)로 배포하려면:

```bash
npm run dist           # dist/ 폴더에 exe/msi 생성 (로컬, 업로드 없음)
npm run release        # 위와 동일 + GitHub Releases에 자동 업로드 (GH_TOKEN 필요)
```

또는 저장소의 **Actions 탭 → Release 워크플로우 → Run workflow**로 클라우드에서 직접 빌드/배포할 수 있습니다.

> ⚠️ C++ 물리 코드를 수정한 경우 반드시 **`npm run build-addon` 재실행** 후 실행하세요.

---

## 🔗 변경 이력 (v3.0.0 요약)

- **codes/2026-7-1(4)**: MSI 설치파일 추가, GitHub Actions 자동 배포, 앱 아이콘, husky/CI 버그 수정
- **codes/2026-7-1(3)**: GitHub Releases publish 설정 확정
- **codes/2026-7-1(2)**: Web Worker 병렬 시뮬레이션, 타입 체크 활성화, C++ 단위 테스트, 물체간 충돌, 프리셋 JSON, CSV 요약, 자동 업데이트
- **codes/2026-7-1**: 다중 물체 시간차 낙하, 전체 UI 개선
- **codes/2026-6-30**: 물리 엔진 고도화(Reynolds·물 저항·Coriolis·중력·회전 감쇠·바운스), 개발 도구 10종

각 폴더의 `README.md`에 더 자세한 변경 내역이 있습니다.

---
---

# 이전 릴리즈 노트 (v2.0.0 이전 히스토리)

## ✨ 2026-6-29(3) 핵심 변경

- **메뉴가 실제로 동작**: (2)의 File/Edit/View 메뉴가 작동하지 않던 문제 해결. 창 상단에 **HTML 메뉴바**를 추가해 클릭=사용자 제스처로 파일 업로드/별도 창 열기/패널 상시 표시가 확실히 동작. 별도 창·IPC 동기화는 그대로 유지.

---

## ✨ 2026-6-29(2) 핵심 변경 (대규모 개편)

- **네이티브 메뉴바 + 별도 설정 창**: File/Edit/View 메뉴. 설정은 창 밖으로 자유 이동 가능한 별도 창으로 열림(메인 렌더러가 단일 출처, IPC 동기화).
- **결과창**: 시뮬 종료 시 자동 표시(별도 창). 종단속도 등 + PNG/CSV 내보내기. 자동 표시 끄기 가능.
- **대기 연속 그라데이션**: 층 경계 없는 매끄러운 sky 셰이더.
- **표면온도 한계 → 열 파괴**: 한계 초과 시 "타서 소멸" 또는 "공중 분해"(Edit→Others에서 선택·임계온도 설정).
- **재료별 파편 차등**: 유리=잘게 다수, 금속=3조각 분리, 목재/콘크리트=청크.
- **프레임레이트 상한 설정**, **패널(설정/그래프/궤적표) 상시 표시 토글**, **크레이터 끄기 토글**.

> **C++ 물리 코드 변경 → `npm run build-addon` 재실행 필요.**

---

## ✨ 2026-6-29(1) 핵심 변경

- **고고도 낙하 재생 끊김/느려짐 수정** — 아주 높은 곳에서 떨어뜨리면 처음엔 잘 내려가다가 어느 순간부터 뚝뚝 끊기던 문제. 표시용 프레임을 최대 3000개로 다운샘플해(요약·충돌 정확도는 유지) 그래프·표·궤적·재생 비용을 낙하 높이와 무관하게 일정하게 만듦.
- **troposphere 구름 표시** — 구름이 시각 낙하 구간 위쪽에만 있어 하단(troposphere)에선 안 보이던 문제. 구름을 낙하 구간 전체에 분포시키고 드롭 높이에 맞춰 재배치 → troposphere 를 지날 때 구름 사이로 떨어지는 것이 보임.

---

## ✨ 2026-6-29 핵심 변경

- **GLB 바닥 잠김 수정** — GLB 모델을 넣으면 절반이 바닥/타겟에 잠겨 보이던 문제. 모델 높이의 절반만큼 자동으로 위로 올려 바닥이 표면에 닿게 함(모델·크기마다 자동 조절).
- **유리(취성) 충격 물리 보정** — 유리 12mm가 8만 kN 이상을 버티던 비현실적 결과 수정. `calcImpact` 에 취성 충격 취약 계수를 도입해 `shatter` 재료(유리)는 충격에 쉽게 깨지도록 함(연성/일반 재료는 영향 없음). **→ `npm run build-addon` 재실행 필요.**

---

## ✨ 2026-6-28 핵심 변경

- **판 휨 (Plate Bend, 토글)** — 공이 떨어졌을 때 재질이 버티면(WITHSTOOD) 판이 강판처럼 충격 지점이 아래로 부드럽게 휘어집니다. 휨 깊이는 충격 운동에너지 / (항복강도 × 두께²) 에 비례하며, 휨 강도 슬라이더로 ±조절. Target 의 `BoxGeometry` 에 윗면 세그먼트를 추가하고 가우시안 변위를 vertex 에 누적 적용.
- **추적(팔로업) 카메라 개선** — 추적 버튼을 눌러도 카메라가 공을 잘 따라가지 못하던 문제 해결. 진입 시 보기 좋은 각도(theta=0.6, phi=1.15, radius=28)로 자동 정렬, 첫 프레임부터 공을 화면 가운데로 스냅, 착지 후에도 부드럽게 추적 유지, 감쇠 계수와 동적 시야 거리 보정.
- **좁은 패널에서 토글 라벨 깨짐 수정** — `바닥에서 띄우기 (지지대 위에 올림)` 라벨이 글자 단위로 세로 겹쳐 보이던 CSS 버그 수정 (`.tog-row` / `.tog-label` 분리, `word-break: keep-all`).

---

## 🆕 2026-6-23(2) → 2026-6-27 누적 변경 사항

이전 안정 버전(`2026-6-23(2)`) 이후 추가/개선된 내용을 한 번에 정리했습니다.
세부 변경 이력은 각 폴더의 `README.md` 를 참고하세요.

### 1. 3D 모델(GLB / STL) 워크플로 전면 강화 (가장 큰 변화)
이전 버전에는 GLB/STL 모델 자체가 없었습니다. 이번 묶음에서 모델 로딩·시각화·물리 연동을 일관된 흐름으로 새로 만들었습니다.
- **GLB 모델 선택 (F17)** — `3D Model` 섹션 드롭다운에서 `assets/` 폴더의 GLB를 즉시 적용. 크기 자동 정규화, 낙하·바운스·충돌과 위치 동기화.
- **STL 업로드 지원 (신규)** — `STLLoader` 추가, 확장자(`.stl` / `.glb`)에 맞는 로더 자동 선택, STL geometry 는 표준 머티리얼 Mesh 로 변환되어 GLB와 동일한 정렬·정규화 로직을 탐.
- **프리셋 모델 3종 제공** — `Classic_table.glb`, `screwdriver.glb`, `Hyperbolic.glb` 가 `assets/` 에 동봉. (이전에는 모델 자체가 없었고 항상 로드 실패하던 `helicopter.glb` 프리셋은 이번에 정리·제거.)
- **GLB 프리셋 물리값 자동 설정** — 프리셋 선택 시 mass·area·Cd 가 모델에 맞게 자동 입력(수동 수정 가능).
- **Shape Custom (GLB)** — Shape 드롭다운에 `Custom (GLB)` 추가. 선택 시 프리셋 + 파일 업로드 UI가 바로 나타남.
- **Multi-Object GLB** — 추가 오브젝트(Object 2/3)에도 각자 GLB 모델을 지정해 같은 물리 궤도로 낙하시키기 가능.
- **모델 크기 조정 (비율 유지) — 신규** — 그룹 래핑 + 균등 스케일로 크기를 바꿔도 가로·세로·높이 비율과 중심이 그대로. `슬라이더 최소/최대 ×` 입력칸으로 슬라이더 범위를 직접 정하고, `크기 직접 입력 ×` 칸으로 배율을 숫자로 입력 가능(범위 밖이면 슬라이더 범위가 자동 확장).
- **업로드 파일을 assets 폴더로 자동 복사** — 내 PC 의 GLB/STL 업로드 시 메인 프로세스가 `assets/` 로 복사한 뒤 로컬 HTTP 서버 정적 경로에서 로드 → **blob URL 로딩 지연 제거**, 다음 실행 때 재사용 가능. (복사 불가 환경은 blob 폴백, 파일명은 안전 문자 치환으로 경로 탈출 방지.)
- **양면 렌더링(DoubleSide)** — 노멀이 뒤집힌 모델도 한쪽 면만 잘려 보이지 않도록 보정.
- **GLB 로딩 진단 토스트** — 로딩 중 / 완료 / 실패+원인+경로를 화면 토스트로 표시. 빠른 연속 선택 시 마지막 요청만 반영(시퀀스 가드).
- **패키지 빌드 GLB 안정화** — `.asar` 안에서 ESM 동적 fetch 가 실패하던 문제를 `build.asar: false` + esbuild 번들로 근본 차단. dev / 패키지 모두 동일 경로.

### 2. 물리 / 충돌 계산 확장
- **공력 가열 계산 (F16, 토글)** — 마하수 기반 정체 온도, Sutton-Graves 열유속, 복사 평형 표면 온도. 공 색상이 파랑→주황→빨강으로 변함. 오버레이/데이터 테이블에 Surface T · Heat Flux 추가.
- **파편 충돌 판정 (토글)** — `stepFragmentsJS` 를 중력·지면 / 파편 간 구 충돌 / 회전 업데이트 3단계로 재구성. 법선 방향 겹침 해소 + 탄성 충격량(반발 0.45) + 회전 교란으로 자연스러운 분산.
- **타겟 바닥에서 띄우기 (토글)** — `바닥에서 띄우기` 토글 + `띄울 높이` 슬라이더 추가. 켜면 네 개의 지지대 기둥 위에 타겟이 올라가 아래 공간이 생기고, 낙하 물체·GLB·멀티 오브젝트 전부 올라간 타겟 표면 기준으로 낙하.

### 3. 안정성 · 시각화 버그 수정
- **렌더링 전체 멈춤 수정** — GLTFLoader 가 bare specifier `'three'` 를 import 하는데 import map 이 없어 async 초기화가 중단되던 문제. import map 추가 + GLTFLoader 로드를 `try/catch` 로 감싸 실패해도 핵심 렌더링은 유지.
- **카메라 팔로업 수정** — 주 물체 착지 이후 카메라가 지면으로 급격히 줌인되던 문제. 착지 후 추적 일시 정지로 부가 오브젝트 낙하 중에도 시야 유지.
- **멀티 오브젝트 낙하 중 사라짐 수정** — 주 물체가 먼저 착지하면 나머지가 강제로 숨겨지던 문제 해결. 전체 오브젝트 최대 낙하 시간까지 재생.

### 4. 빌드 · 배포
- **arm64 지원** — Windows 플랫폼이 x64 + arm64 두 아키텍처로 확장.
- **esbuild 번들** — `renderer.js` + `three.js` + `GLTFLoader` 를 한 파일(`renderer.bundle.js`)로 번들. 런타임 동적 import 자체를 없애 패키지 앱에서 *"Failed to fetch dynamically imported module"* 오류 원천 차단.
- **`.stl` MIME 추가** — 로컬 HTTP 서버에서 STL 도 표준 MIME 으로 서빙.

---

## 🔗 변경 이력 (v2.0.0 이전 요약)

- **2026-6-29(3)**: 메뉴 실제 동작(창 안 HTML 메뉴바 추가 — 파일 업로드/별도 창/패널 상시 표시)
- **2026-6-29(2)**: 네이티브 메뉴/별도 설정·결과 창, 대기 연속 그라데이션, 열 파괴(소멸/분해), 재료별 파편, 프레임레이트 상한, 패널 상시 표시
- **2026-6-29(1)**: 고고도 낙하 재생 끊김/느려짐 수정(프레임 다운샘플), troposphere 구름 표시
- **2026-6-29**: GLB 바닥 잠김 수정(모델 높이만큼 자동 보정), 유리(취성) 충격 물리 보정
- **2026-6-28**: 판 휨(Plate Bend, 토글), 추적 카메라 팔로업 개선, 좁은 패널 토글 라벨 깨짐 수정
- **2026-6-27**: 모델 크기 슬라이더(비율 유지·범위·수치 입력), STL 렌더, 업로드 assets 복사, helicopter.glb 프리셋 정리
- **2026-6-25(2)**: Shape Custom (GLB), GLB 진단 토스트, 타겟 바닥에서 띄우기, Hyperbolic.glb 추가, 패키지 빌드 GLB 안정화
- **2026-6-25(1)**: 카메라 팔로업 수정, Multi-Object GLB, GLB 프리셋 물리값 자동 설정
- **2026-6-25**: 렌더링 전체 멈춤 수정(import map / try-catch), 멀티 오브젝트 낙하 중 사라짐 수정
- **2026-6-23(4)**: Classic_table.glb · screwdriver.glb 프리셋 추가
- **2026-6-23(3)**: 공력 가열(F16), 파편 충돌 판정, GLB 모델 도입(F17)
- **2026-6-23(2)**: 설정창 폭 자유 조절(토글) — *v1.0.0 기준 버전*

각 버전별 상세 내역은 해당 폴더의 `README.md`를 참고하세요.
